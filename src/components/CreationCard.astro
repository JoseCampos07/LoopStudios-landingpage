---
const { image, title } = Astro.props;
---

<div class="relative w-60">
    <img src={ image } alt={title}>
    <h1 class="absolute bottom-2 left-2 w-10 text-3xl text-white font-light ml-2 mb-2">
        { title }
    </h1>
</div>